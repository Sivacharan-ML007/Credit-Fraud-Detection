# Credit-Fraud-Detection
This script builds an unsupervised anomaly detection model using a deep learning autoencoder in TensorFlow.  The idea:  Train the model only on normal transactions Let it learn how “normal” data looks Flag transactions as fraud when reconstruction error is high
